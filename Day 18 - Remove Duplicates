// given a function we have to remove duplicates found in the array.

int removeDuplicates(vector<int> &arr, int n) {
	// Write your code here.
	vector<int> result;
	for(int i=0;i<=n-1;i++){
		if(arr[i]!=arr[i+1]){
			result.push_back(arr[i]);
		}
	}
	int ans = result.size();
	return ans;
}
